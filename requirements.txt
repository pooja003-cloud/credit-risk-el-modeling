# Core data and modelling
pandas>=2.2,<4
numpy>=1.26,<3
scikit-learn>=1.6,<2        # sklearn.frozen.FrozenEstimator requires >=1.6
pyarrow>=15                 # parquet engine for the processed splits
joblib>=1.3

# Gradient boosting (either is enough; the ladder skips whichever is absent)
xgboost>=2.0
lightgbm>=4.0

# Explainability
shap>=0.44

# Figures
matplotlib>=3.8

# Dashboards
streamlit>=1.49             # width="stretch" replaced use_container_width in 1.49
plotly>=5.20

# Notebook and tests
jupyter>=1.0
pytest>=8.0
